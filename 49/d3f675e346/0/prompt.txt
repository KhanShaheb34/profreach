Implement the following plan:

# Profreach Implementation Plan

## Overview
Build a professor outreach tracker for grad school applications using Next.js 16 (App Router), TypeScript, Tailwind CSS v4 + shadcn/ui, Gemini API, and localStorage.

---

## Phase 0: Setup & Dependencies
- Install: `@google/generative-ai`, `uuid`, `@types/uuid`, `lucide-react`, `sonner`
- Init shadcn/ui (New York style, CSS variables)
- Add shadcn components: button, input, label, textarea, select, badge, card, dialog, dropdown-menu, tabs, separator, scroll-area, sheet, tooltip, skeleton, toast, popover, command
- Create `.env.local` with `GEMINI_API_KEY`
- **Commit**: `chore: install dependencies and initialize shadcn/ui`

## Phase 1: Foundation (Types, Storage, Layout)

### Files to create:
- `lib/types.ts` — All interfaces (Professor, Profile, Document, MemoryItem, ChatMessage, EmailDraft, AppData) + enums (ApplicationStatus, HiringStatus, DocumentCategory, EmailTemplate)
- `lib/constants.ts` — Status colors, default profile, sort options
- `lib/storage.ts` — localStorage abstraction with custom event dispatch for reactivity. Collections: professors, profile, documents, memory, chats, drafts. Includes exportAll/importAll.
- `lib/gemini.ts` — Server-side Gemini client init (standard model + grounded search model)
- `hooks/use-storage.ts` — `useSyncExternalStore`-based hook that re-renders on storage changes
- `hooks/use-debounce.ts` — Debounce hook for search (300ms)
- `components/providers.tsx` — Client providers (Toaster from sonner)
- `components/layout/sidebar.tsx` — Navigation sidebar (Dashboard, Profile, Export)
- `components/layout/app-shell.tsx` — Sidebar + content wrapper
- Update `app/layout.tsx` — Wrap with Providers + AppShell, update metadata
- Update `app/globals.css` — Custom design tokens, distinctive color palette
- **Commit**: `feat: add type definitions, storage layer, layout shell, and navigation`

## Phase 2: Dashboard
- `app/page.tsx` — Dashboard page (server wrapper)
- `app/api/gemini/lookup/route.ts` — Professor lookup via Gemini with Google Search grounding
- `components/dashboard/dashboard-stats.tsx` — Summary stat cards
- `components/dashboard/add-professor-input.tsx` — Input + Gemini lookup trigger
- `components/dashboard/add-professor-dialog.tsx` — Review/edit AI results before saving
- `components/dashboard/filter-bar.tsx` — Search, filter (status/country/area/hiring), sort controls
- `components/dashboard/professor-list.tsx` — Card grid, applies filters/sorting
- `components/dashboard/professor-card.tsx` — Single card with quick status change
- `components/dashboard/status-badge.tsx` — Colored status badge (reusable)
- **Commit**: `feat: implement dashboard with professor list, filtering, and AI lookup`

## Phase 3: Professor Detail Page
- `app/professor/[id]/page.tsx` — Server wrapper (await params.id)
- `app/api/gemini/email/route.ts` — Email draft generation
- `app/api/gemini/chat/route.ts` — Professor-scoped chat (streaming)
- `app/api/gemini/memory/route.ts` — Memory extraction from chat exchanges
- `components/professor/professor-detail.tsx` — Main layout (info + email + chat)
- `components/professor/professor-info-panel.tsx` — Editable professor fields
- `components/professor/tag-input.tsx` — Reusable tag/chip input
- `components/professor/email-draft-section.tsx` — Template selector, generate, edit, copy
- `components/professor/chat-section.tsx` — Chat UI with auto-memory extraction
- `components/professor/chat-message.tsx` — Message bubble
- **Commit**: `feat: implement professor detail with info editing, email drafting, and AI chat`

## Phase 4: Profile Page
- `app/profile/page.tsx` — Server wrapper with tabs
- `app/api/gemini/resume/route.ts` — Resume PDF parsing via Gemini
- `components/profile/profile-form.tsx` — Personal info form (auto-saves)
- `components/profile/resume-upload.tsx` — PDF upload + parse + apply to profile
- `components/profile/document-manager.tsx` — Document list by category
- `components/profile/document-upload-dialog.tsx` — Upload with category selector
- `components/profile/smart-memory.tsx` — Memory list, search, add/edit/delete
- `components/profile/memory-item-card.tsx` — Single memory card
- **Commit**: `feat: implement profile page with resume parsing, documents, and smart memory`

## Phase 5: Export & Polish
- `app/export/page.tsx` — Export page
- `components/export/export-controls.tsx` — Export JSON, import JSON, data summary
- Dynamic imports for heavy components (ChatSection, SmartMemory, DocumentManager)
- Loading skeletons, empty states, responsive design (sidebar collapses on mobile)
- Dark mode support via CSS variables
- Toast notifications for all operations
- **Commit**: `feat: implement data export and final polish`

---

## Key Architecture Decisions
1. **Storage events**: Custom DOM event on every write; `useSyncExternalStore` subscribes to it for reactivity
2. **Server vs Client**: Pages are thin server wrappers; all data components are `'use client'` (localStorage)
3. **AI flow**: Client collects data -> POST to `/api/gemini/*` -> Gemini call -> structured JSON response -> client writes to storage
4. **Chat streaming**: `/api/gemini/chat` returns ReadableStream for progressive rendering
5. **Memory extraction**: Fire-and-forget after each assistant response, subtle toast on extraction

## Verification
1. Run `pnpm dev` and verify all pages render
2. Add a professor via the lookup input, verify Gemini returns structured data
3. Edit professor info on detail page, verify localStorage persists
4. Generate an email draft, verify it uses professor + profile context
5. Chat on a professor page, verify memory extraction works
6. Upload a resume PDF, verify parsing populates profile
7. Upload documents, verify categorization and download
8. Export JSON, verify all data is included
9. Test responsive layout on mobile viewport


If you need specific details from before exiting plan mode (like exact code snippets, error messages, or content you generated), read the full transcript at: /Users/shakirulhkhan/.REDACTED.jsonl

---

Getting this error when I open the app on browser: ## Error Type
Console Error

## Error Message
The result of getSnapshot should be cached to avoid an infinite loop


    at DashboardStats (components/dashboard/dashboard-stats.tsx:10:32)
    at DashboardContent (components/dashboard/dashboard-content.tsx:17:7)

## Code Frame
   8 |
   9 | export function DashboardStats() {
> 10 |   const professors = useStorage(getProfessors);
     |                                ^
  11 |
  12 |   if (!professors) return null;
  13 |

Next.js version: 16.1.6 (Turbopack)

---

Good job so far. A few adjustments:
1. I want the chat to be a floating circle on the right bottom corner of the page, by clicking it a floating window will open on the corner to chat. Kind of like messaging inside facebook or instagram. And make the professor data full width.
2. The smart memory box does not grow as more items are added.
3. I want a settings page where user will be able to set an api key for gemini. Currently we take it from .env.local, but if I share the website with others, I might get bankrupt if others start using it now. So keep an api key input box along with how to get an api key from aistudio on the settings page, and instead of taking the api key from .env, take it from there, probably saving it to the localstorage. If api key is not set the whole website will have a banner on top with an input box to set the api key. Make sure the ux is good enough so that users can understand this properly.

---

<bash-input>pnpm dlx shadcn@latest add https://tweakcn.com/r/themes/cmlpj7uaz000204la8bw7gu80</bash-input>

---

<bash-stdout>[?25l[2K[1G[36m?[39m [1mYou are about to install a new style. 
Existing CSS variables and components will be overwritten. Continue?[22m [90m›[39m [90m(y/N)[39m</bash-stdout><bash-stderr></bash-stderr>

---

I've added a different theme for the app, but that is not applied on the actual design. Why is that? Check @app/globals.css for the change.

---

Update the @README.md of the repo

---

This session is being continued from a previous conversation that ran out of context. The summary below covers the earlier portion of the conversation.

Analysis:
Let me chronologically analyze the conversation:

1. **Initial Setup**: User provided a detailed implementation plan for "Profreach" - a professor outreach tracker for grad school applications. The plan had 6 phases (0-5).

2. **Phase 0**: Installed dependencies (`@google/generative-ai`, `uuid`, `lucide-react`, `sonner`, `@types/uuid`), initialized shadcn/ui, added 18 shadcn components. Had to work around shadcn init prompts. `toast` component was deprecated, used `sonner` instead. Created `.env.local`.

3. **Phase 1**: Created foundation files - types, constants, storage, gemini client, hooks, providers, layout components. Updated app/layout.tsx and globals.css.

4. **Phase 2**: Created dashboard with stats, professor list, filtering, AI lookup via Gemini with Google Search grounding. Created API route `/api/gemini/lookup`.

5. **Phase 3**: Created professor detail page with info panel, email drafting, streaming chat, memory extraction. Created API routes for email, chat, memory.

6. **Phase 4**: Created profile page with resume upload/parsing, document manager, smart memory. Had a TypeScript error with `useRef` needing an initial argument - fixed by passing `undefined`.

7. **Phase 5**: Created export page, dark mode toggle, theme detection script.

8. **First user feedback - infinite loop error**: `useSyncExternalStore` was causing infinite re-renders because `getProfessors()` returns a new array reference from `JSON.parse` every call. Fixed by caching the snapshot using JSON string comparison in a ref.

9. **Second user feedback - three adjustments**:
   - Chat should be a floating circle/bubble (like Messenger/Instagram) instead of embedded in the grid
   - Smart memory box doesn't grow as items are added
   - Settings page for API key management instead of .env.local

   For this I:
   - Rewrote chat-section.tsx as a floating overlay with bubble, minimize, close
   - Made professor detail full-width (removed the 2-column grid)
   - Removed ScrollArea with max-height from smart-memory.tsx
   - Created settings page with API key input, instructions, privacy note
   - Created API key banner component that shows when no key is set
   - Updated lib/gemini.ts to accept apiKey as parameter
   - Updated ALL API routes to accept apiKey from request body
   - Updated ALL client components that make fetch calls to include getApiKey()
   - Added getApiKey/setApiKey to storage.ts
   - Added Settings to sidebar nav

10. **Third user feedback - theme not applied**: User added a tweakcn theme that changed CSS variables to use IBM Plex Sans fonts and a warm color palette, but fonts weren't loading because layout.tsx still imported Geist fonts. Fixed by:
    - Replacing Geist imports with IBM_Plex_Sans, IBM_Plex_Mono, IBM_Plex_Serif via next/font/google
    - Updated @theme inline block to reference the next/font CSS variables
    - Removed redundant font declarations from :root and .dark blocks

11. **Fourth user request**: Update README.md - replaced the default create-next-app README with a comprehensive project README covering features, setup, API key instructions, tech stack, and project structure.

Let me now compile the summary with all the important details.

Summary:
1. Primary Request and Intent:
   The user requested a full implementation of "Profreach" — a professor outreach tracker for grad school applications — following a detailed 6-phase plan. The app uses Next.js 16 (App Router), TypeScript, Tailwind CSS v4, shadcn/ui, Google Gemini API, and localStorage. After the initial build, the user requested three adjustments: (1) floating chat bubble like Messenger/Instagram, (2) smart memory box that grows with content, (3) settings page for user-provided Gemini API key instead of .env.local. The user also applied a custom tweakcn theme and needed font loading fixed, and finally requested an updated README.md.

2. Key Technical Concepts:
   - Next.js 16 App Router with server page wrappers and `'use client'` data components
   - `useSyncExternalStore` for reactive localStorage (with JSON-based snapshot caching to avoid infinite loops)
   - Custom DOM event (`profreach-storage-change`) dispatched on every localStorage write for reactivity
   - Google Gemini 2.0 Flash via `@google/generative-ai` with Google Search grounding for professor lookup
   - Streaming chat responses via `ReadableStream` from API routes
   - Fire-and-forget memory extraction after each chat exchange
   - Client-side API key management (stored in localStorage, sent per-request, never on server)
   - Tailwind CSS v4 with `@theme inline` and `@custom-variant dark` for dark mode
   - `next/font/google` for font loading (IBM Plex Sans/Mono/Serif)
   - shadcn/ui New York style with 18 components

3. Files and Code Sections:

   - **`lib/types.ts`** — All interfaces and enums (Professor, Profile, Document, MemoryItem, ChatMessage, EmailDraft, AppData, ApplicationStatus, HiringStatus, DocumentCategory, EmailTemplate)

   - **`lib/storage.ts`** — localStorage abstraction with custom event dispatch. Key addition was `getApiKey`/`setApiKey`:
     ```typescript
     const KEYS = {
       professors: "profreach:professors",
       profile: "profreach:profile",
       documents: "profreach:documents",
       memory: "profreach:memory",
       chats: "profreach:chats",
       drafts: "profreach:drafts",
       apiKey: "profreach:gemini-api-key",
     } as const;
     
     export function getApiKey(): string {
       return get(KEYS.apiKey, "");
     }
     export function setApiKey(key: string) {
       set(KEYS.apiKey, key);
     }
     ```

   - **`lib/gemini.ts`** — Changed from server-side singleton to per-request instantiation accepting apiKey parameter:
     ```typescript
     import { GoogleGenerativeAI } from "@google/generative-ai";
     export function getModel(apiKey: string) {
       if (!apiKey) throw new Error("Gemini API key not configured");
       const genAI = new GoogleGenerativeAI(apiKey);
       return genAI.getGenerativeModel({ model: "gemini-2.0-flash" });
     }
     export function getGroundedModel(apiKey: string) {
       if (!apiKey) throw new Error("Gemini API key not configured");
       const genAI = new GoogleGenerativeAI(apiKey);
       return genAI.getGenerativeModel({
         model: "gemini-2.0-flash",
         tools: [{ googleSearch: {} } as never],
       });
     }
     ```

   - **`hooks/use-storage.ts`** — Critical fix for infinite loop. Uses JSON string caching:
     ```typescript
     "use client";
     import { useSyncExternalStore, useCallback, useRef } from "react";
     import { subscribe } from "@/lib/storage";
     export function useStorage<T>(getter: () => T): T {
       const cacheRef = useRef<{ json: string; value: T } | null>(null);
       const getSnapshot = useCallback(() => {
         const value = getter();
         const json = JSON.stringify(value);
         if (cacheRef.current && cacheRef.current.json === json) {
           return cacheRef.current.value;
         }
         cacheRef.current = { json, value };
         return value;
       }, [getter]);
       const getServerSnapshot = useCallback((): T => {
         return undefined as unknown as T;
       }, []);
       return useSyncExternalStore(subscribe, getSnapshot, getServerSnapshot);
     }
     ```

   - **`hooks/use-debounce.ts`** — Simple debounce hook (300ms default)

   - **`lib/constants.ts`** — STATUS_COLORS, STATUS_LABELS, HIRING_STATUS_LABELS, DOCUMENT_CATEGORY_LABELS, EMAIL_TEMPLATE_LABELS, DEFAULT_PROFILE, SORT_OPTIONS

   - **`components/providers.tsx`** — Wraps Toaster (sonner) + TooltipProvider

   - **`components/layout/sidebar.tsx`** — Navigation sidebar with Settings link, ThemeToggle (dark/light), responsive mobile sheet
   
   - **`components/layout/app-shell.tsx`** — Sidebar + ApiKeyBanner + content wrapper:
     ```typescript
     "use client";
     import { Sidebar } from "./sidebar";
     import { ApiKeyBanner } from "./api-key-banner";
     export function AppShell({ children }: { children: React.ReactNode }) {
       return (
         <div className="min-h-screen">
           <Sidebar />
           <div className="md:ml-60">
             <div className="pt-14 md:pt-0">
               <ApiKeyBanner />
             </div>
             <main>
               <div className="container mx-auto max-w-6xl p-4 md:p-8">
                 {children}
               </div>
             </main>
           </div>
         </div>
       );
     }
     ```

   - **`components/layout/api-key-banner.tsx`** — Amber banner at top when no API key set, with inline input, link to AI Studio, dismiss button

   - **`app/layout.tsx`** — Updated to load IBM Plex fonts via next/font/google instead of Geist:
     ```typescript
     import { IBM_Plex_Sans, IBM_Plex_Mono, IBM_Plex_Serif } from "next/font/google";
     const plexSans = IBM_Plex_Sans({ variable: "--font-ibm-plex-sans", subsets: ["latin"], weight: ["400", "500", "600", "700"] });
     const plexMono = IBM_Plex_Mono({ variable: "--font-ibm-plex-mono", subsets: ["latin"], weight: ["400", "500", "600"] });
     const plexSerif = IBM_Plex_Serif({ variable: "--font-ibm-plex-serif", subsets: ["latin"], weight: ["400", "500", "600", "700"] });
     ```
     Also includes dark mode detection script in `<head>`.

   - **`app/globals.css`** — User applied a tweakcn theme with warm oklch color palette. Key fix was wiring `@theme inline` font vars to next/font CSS variables:
     ```css
     --font-sans: var(--font-ibm-plex-sans), "IBM Plex Sans", "Inter", ui-sans-serif, system-ui, -apple-system, sans-serif;
     --font-mono: var(--font-ibm-plex-mono), "IBM Plex Mono", "Fira Code", ui-monospace, monospace;
     --font-serif: var(--font-ibm-plex-serif), "IBM Plex Serif", "Georgia", ui-serif, serif;
     ```
     Redundant `--font-*` declarations removed from `:root` and `.dark` blocks.

   - **Dashboard components** (`components/dashboard/`):
     - `dashboard-content.tsx` — Client wrapper for dashboard page
     - `dashboard-stats.tsx` — Summary stat cards (total, sent, replied, accepted)
     - `add-professor-input.tsx` — Input with AI lookup trigger, sends `apiKey: getApiKey()` in fetch
     - `add-professor-dialog.tsx` — Review/edit AI results before saving
     - `filter-bar.tsx` — Search, status filter, sort controls
     - `professor-list.tsx` — Card grid with filtering/sorting
     - `professor-card.tsx` — Single card with quick status change dropdown
     - `status-badge.tsx` — Colored status badge

   - **Professor components** (`components/professor/`):
     - `professor-detail.tsx` — Full-width layout (info + email stacked vertically), floating ChatSection overlay
     - `professor-info-panel.tsx` — Editable professor fields with Save button
     - `tag-input.tsx` — Reusable tag/chip input with Enter/comma to add, Backspace to remove
     - `email-draft-section.tsx` — Template selector, generate, edit, copy, save drafts. Sends `apiKey: getApiKey()`
     - `chat-section.tsx` — **Floating chat bubble** (fixed bottom-right, opens overlay window with minimize/close). Sends apiKey in all fetch calls
     - `chat-message.tsx` — Message bubble component

   - **Profile components** (`components/profile/`):
     - `profile-content.tsx` — Tabs wrapper (Personal Info, Documents, Smart Memory)
     - `profile-form.tsx` — Auto-saving profile form with 1s debounce
     - `resume-upload.tsx` — PDF upload with drag-and-drop, sends apiKey in FormData
     - `document-manager.tsx` — Document list with upload/download/delete
     - `document-upload-dialog.tsx` — Upload with category selector
     - `smart-memory.tsx` — Memory list that **grows with items** (removed fixed max-height ScrollArea)
     - `memory-item-card.tsx` — Inline edit/delete for memory items

   - **Settings components** (`components/settings/`):
     - `settings-content.tsx` — API key input, show/hide toggle, delete, step-by-step instructions for getting a key from Google AI Studio, privacy note

   - **Export components** (`components/export/`):
     - `export-content.tsx` — Wrapper
     - `export-controls.tsx` — Data summary stats, export JSON, import JSON

   - **API Routes** (all updated to accept `apiKey` from request body):
     - `app/api/gemini/lookup/route.ts` — Professor lookup with Google Search grounding
     - `app/api/gemini/email/route.ts` — Email draft generation
     - `app/api/gemini/chat/route.ts` — Streaming chat with ReadableStream
     - `app/api/gemini/memory/route.ts` — Memory extraction from chat exchanges
     - `app/api/gemini/resume/route.ts` — Resume PDF parsing (apiKey via FormData)

   - **`README.md`** — Updated from default create-next-app to comprehensive project README with features, setup, API key instructions, tech stack, and project structure

4. Errors and Fixes:
   - **shadcn init `--style` flag not recognized**: Used `--defaults` flag instead
   - **shadcn `toast` component deprecated**: Used `sonner` component instead
   - **`useRef` TypeScript error** (`Expected 1 arguments, but got 0`): Fixed by passing `undefined` as initial value: `useRef<ReturnType<typeof setTimeout>>(undefined)`
   - **Infinite loop with `useSyncExternalStore`**: `getSnapshot` returned new object references from `JSON.parse` on every call. Fixed by caching snapshots in a ref and comparing JSON strings to return stable references.
   - **Theme not applied after tweakcn install**: The tweakcn theme set `--font-sans: "IBM Plex Sans"` but no font files were loaded (layout.tsx still imported Geist). Fixed by replacing Geist with IBM Plex font imports via `next/font/google` and wiring `@theme inline` variables to use `var(--font-ibm-plex-sans)` etc. Removed redundant font declarations from `:root` and `.dark`.
   - **Auto-checkpoint hook**: An external hook auto-committed changes, so some manual `git add` commands found files already committed.

5. Problem Solving:
   - Solved the core architectural challenge of reactive localStorage using `useSyncExternalStore` with custom DOM events and JSON-based snapshot caching
   - Solved the API key security concern by moving from server-side .env to client-side localStorage, with per-request key passing through all API routes
   - Solved the chat UX by converting from embedded card to floating overlay (like Messenger)
   - Solved the theme integration by properly loading IBM Plex fonts via next/font and wiring CSS variables

6. All User Messages:
   - "Implement the following plan: [detailed 6-phase implementation plan for Profreach]"
   - "Getting this error when I open the app on browser: The result of getSnapshot should be cached to avoid an infinite loop at DashboardStats"
   - "Good job so far. A few adjustments: 1. I want the chat to be a floating circle on the right bottom corner of the page... 2. The smart memory box does not grow as more items are added. 3. I want a settings page where user will be able to set an api key for gemini..."
   - "I've added a different theme for the app, but that is not applied on the actual design. Why is that? Check @app/globals.css for the change."
   - "Update the @README.md of the repo"

7. Pending Tasks:
   - No explicit pending tasks. All user requests have been addressed.

8. Current Work:
   The most recent work was updating the README.md file from the default create-next-app boilerplate to a comprehensive project README covering all features, setup instructions, API key configuration, tech stack, and project structure. This was the user's last explicit request: "Update the @README.md of the repo". The file was written successfully.

9. Optional Next Step:
   No next step needed. All explicit user requests have been completed. The last task (README update) is done. Waiting for further user instructions.

If you need specific details from before compaction (like exact code snippets, error messages, or content you generated), read the full transcript at: /Users/shakirulhkhan/.REDACTED.jsonl
Please continue the conversation from where we left it off without asking the user any further questions. Continue with the last task that you were asked to work on.

---

<bash-input>pnpm i @vercel/analytics</bash-input>

---

<bash-stdout>Progress: resolved 0, reused 1, downloaded 0, added 0
 WARN  deprecated @types/uuid@11.0.0: This is a stub types definition. uuid provides its own type definitions, so you do not need this installed.
Progress: resolved 28, reused 27, downloaded 0, added 0
 WARN  1 deprecated subdependencies found: node-domexception@1.0.0
Packages: +1
+
Progress: resolved 748, reused 679, downloaded 1, added 1, done

dependencies:
+ @vercel/analytics 1.6.1

╭ Warning ─────────────────────────────────────────────────────────────────────╮
│                                                                              │
│   Ignored build scripts: msw.                                                │
│   Run "pnpm approve-builds" to pick which dependencies should be allowed     │
│   to run scripts.                                                            │
│                                                                              │
╰──────────────────────────────────────────────────────────────────────────────╯

Done in 6.2s using pnpm v10.23.0</bash-stdout><bash-stderr></bash-stderr>